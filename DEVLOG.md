# MermaidGenerator 開發日誌

## 如何開啟此專案（本機開發）

**環境需求**
- Node.js `^20.19.0` 或 `>=22.12.0`（Vite 的最低需求）
- npm（隨 Node.js 安裝）

**啟動步驟**
```bash
cd MermaidGenerator
npm install       # 第一次執行或有新增套件時才需要
npm run dev       # 啟動開發伺服器
```
啟動後，於瀏覽器開啟 `http://localhost:5173` 即可看到專案頁面。畫面支援 HMR，修改 `src/` 內的檔案會自動更新。

**停止伺服器**：在終端機按 `Ctrl+C`。

**其他指令**
```bash
npm run build      # 型別檢查 + 正式建置（輸出至 dist/）
npm run preview    # 預覽 build 後的成果
npm run lint        # 執行 oxlint 檢查
```

---

## 日誌

### 2026-07-31 — 畫布自動整理、浮動面板毛玻璃透明、預設面板寬度調整
- **畫布自動整理**：React Flow 左下角縮放控制列新增一顆「自動整理」按鈕（四方格圖示）。點下去會用一個自己寫的最小位移重疊消除演算法（`src/canvas/autoArrange.ts` 的 `resolveOverlaps`：兩兩偵測邊界框是否重疊，沿重疊量較小的軸把兩者各推開一半，重複到不再重疊或達到疊代上限）分兩層處理：先在每個 subgraph 群組內部把重疊的成員節點分開，再把每個群組（依成員位置算出的外框）跟所有未分組節點放在同一層級再做一次重疊消除，群組整體平移時內部成員一起跟著移動（保持在框內）。刻意選擇「只解決重疊、不做完整階層重排」，所以沒有重疊的節點位置完全不會被打亂，符合手動排列的心血不會被洗掉的需求。新增 store action `applyNodePositions`，一次套用所有變動節點的新座標，讓一次「自動整理」在復原/重做只算一步，不用按好幾次 Ctrl+Z
- **浮動面板毛玻璃透明**：「形狀與顏色」面板展開時背景改為半透明（約 88% 不透明度）加 `backdrop-filter: blur(8px)`，底下的畫布節點/網格若隱若現，面板本身文字與按鈕維持清楚易讀；收合狀態的小標籤按鈕維持不透明白底，避免跟畫布內容混在一起不好點擊
- **三欄預設寬度調整**：程式碼／畫布／預覽從 25% / 25% / 50% 改為 25% / 35% / 40%。這只影響「重設版面」或全新使用者的預設值，已經在瀏覽器存過版面比例的人不會自動被改動，要改的話一樣按「重設版面」
- 已用 Playwright 完整驗證：預設面板寬度比例（25/35/40 實測相符）、面板透明毛玻璃效果、刻意用 localStorage 灌入重疊節點資料觸發自動整理（含群組內重疊與群組外重疊兩種情境）確認整理後不再重疊、群組外框正確跟著重新包覆、未重疊節點不受影響，以及一次 Ctrl+Z／點「復原」即可完整還原整理前的位置，無 console 錯誤

### 2026-07-30 — 完成 Phase 6：打磨與部署（企劃書最後一個階段）
- **自我檢視找出的 UI 打磨項目**：實際跑一輪 Playwright 檢視畫布/程式碼/預覽/浮動面板/色票/面板拖拉手把等各種狀態後，確認大部分細節（按鈕 disabled 樣式、面板陰影、色票、群組操作提示文字、PNG 匯出載入中狀態）已經合格，只挑出兩項有實際證據支持的缺口動手：
  1. **空狀態提示**：畫布或預覽面板完全沒有節點時（例如手動刪光、或程式碼只留 `flowchart TD`），過去會整片空白、容易讓人誤以為當機。畫布加了置中的浮動提示文字，預覽面板加了置中的引導文字
  2. **`<meta name="description">`**：補上一句話介紹，讓分享連結、瀏覽器分頁縮圖有基本說明
- **React 全域錯誤邊界（Error Boundary）**：包在 `main.tsx` 最外層，任何元件在 render 階段丟出未捕捉的例外，不會再整片白畫面，而是顯示一張說明卡片，提供「重新整理」與「清除本機暫存並重新開始」兩個復原路徑（後者會呼叫 Phase 5 就有的 `clearPersistedModel()`，避免壞掉的 localStorage 資料造成無限白畫面循環）。用一個暫時性的 `?crashtest=1` URL 參數手動觸發 render 例外驗證過畫面正確顯示，驗證後即移除該測試用程式碼
- **匯入錯誤訊息 UI 化**：原本 `.mmd`/`.json` 匯入失敗或有無法解析的行時是用瀏覽器原生 `window.alert()` 顯示，體驗生硬且會擋住畫面。改成頁首下方的可關閉提示橫幅（`NoticeBanner`），區分「警告（黃色，匯入成功但部分內容跳過）」與「錯誤（紅色，整個匯入失敗）」兩種樣式，並把訊息內容補上檔名與更具體的原因說明
- **Mermaid 語法錯誤/警告用詞優化**：
  - 程式碼反向解析器（`parseMermaidToModel`）原本對於「看不懂的一行」只會原封不動印出該行文字（`無法解析：xxx`），沒有任何線索；改成明確講「不是目前支援的節點／連線寫法」，讓非技術背景的使用者知道問題方向是語法子集限制，而不是自己打錯字
  - 預覽面板的 Mermaid 渲染錯誤原本直接把 mermaid.js 丟出的技術性錯誤訊息整段顯示；改成先用一句白話說明「這個圖表目前無法正常渲染，可能是節點文字包含 Mermaid 無法辨識的內容，畫布與程式碼內容不會遺失」，技術性原始錯誤訊息收進可展開的「技術細節」裡，需要時才看
- **GitHub Pages 部署準備**：`vite.config.ts` 設定 `base: '/MermaidGenerator/'`，新增 `.github/workflows/deploy.yml`（`actions/checkout` → `npm ci` → `npm run build` → `actions/upload-pages-artifact` → `actions/deploy-pages`，推送到 `main` 分支自動觸發），README 補上部署步驟說明。用 `npm run build` + `npm run preview` 實際跑一次帶 base path 的正式建置結果，Playwright 確認頁面、樣式、字型、Mermaid 渲染都正常載入、無 404 或 console 錯誤
- 這次沒有一起做的：favicon 客製化（目前仍是 Vite/React 預設圖示）留待之後有需要再處理
- **`fish-02.github.io` 被 Chrome／Google Safe Browsing 判定為危險網站，深入排查後確認與本專案程式碼無關**：第一次部署上線後開啟網址被瀏覽器擋下，因此暫時改試過 Vercel，但使用者要求先查清楚根本原因、盡量在不換部署平台的情況下解決。深入排查證據：`npm audit` 0 個漏洞；打包後的 JS 全文搜尋確認沒有 `eval()`／`new Function()`／`document.write`／`<iframe>` 注入；唯一的 `fetch()` 呼叫是 Vite 官方 modulepreload polyfill（同源預載其他程式碼區塊），katex 套件裡看似的 `fetch(` 其實是它 parser 內部 token 物件的方法名稱、與網路請求無關；`dist/index.html` 的所有 `<script>`／`<link>` 都指向自己網域下的 `/assets/...`，沒有任何外部 CDN、追蹤碼或第三方腳本。搜尋後也證實這是 GitHub Pages 上有大量社群討論的已知現象：Safe Browsing 的判定綁在 `使用者名稱.github.io` 這個子網域本身（很可能是帳號名稱或子網域先前被別人使用時留下的殘留標記），不是掃描 repo 裡的檔案內容。結論：改回 GitHub Pages（`vite.config.ts` 的 base path、`.github/workflows/deploy.yml` 都復原），沒有程式碼需要修正；正規解法是透過 Google Search Console 驗證網域擁有權後申請安全性複檢，等 Google 重新掃描

**下一步**：企劃書六個階段已全數完成。後續屬於自由維護期，若有新需求（例如更多節點形狀、循序圖、深色模式等）會在此檔案另開新的階段記錄

### 2026-07-30 — 完成 Phase 5：匯出、匯入與 localStorage 自動暫存
- **localStorage 自動暫存**：`graphStore` 用 zustand 的 `subscribe` 監看 `model` 變化，debounce 500ms 後寫入 `localStorage`；應用程式啟動時會優先讀取這份暫存資料，讀不到（第一次使用、資料損毀）才退回範例資料
- **匯出 `.mmd`**：直接把目前的 Mermaid 序列化字串包成檔案下載
- **匯出 SVG／PNG**：以官方 mermaid.js 渲染出的 SVG 為準（符合「匯出以預覽為準」的原則），新增「透明背景」核取方塊——關閉時會在 SVG 最前面插入一個鋪滿的白色 `<rect>`，開啟時保持原樣（天生透明）
- **PNG 匯出踩到一個瀏覽器安全限制**：Mermaid 產出的 SVG 用 `<foreignObject>` 承載文字標籤，透過 `URL.createObjectURL()` 建立的 blob 網址載入這種 SVG 到 `<img>` 再畫進 `<canvas>` 時，Chromium 會把該 canvas 標記為「tainted（受污染）」，呼叫 `canvas.toBlob()` 時直接丟出安全性錯誤、無法匯出。改用 `data:` URI（`encodeURIComponent` 直接編碼，不用 base64）取代 blob 網址後解決——這是社群普遍回報的 Mermaid → PNG 轉換已知陷阱
- **匯入 `.mmd` / `.json`**：`.mmd` 直接重用 Phase 3 的反向解析器；`.json` 則驗證檔案是否具備 `nodes`/`edges`/`subgraphs` 欄位後直接視為完整的 Graph Model 載入。匯入的檔案會整個取代目前畫面（不是合併），無法解析的行會列出警告
- 頁首新增「匯入」按鈕（隱藏的檔案選取 input），預覽面板新增匯出工具列（透明背景切換 + 三個匯出按鈕）
- 已用 Playwright 完整驗證：改內容→重新整理→確認暫存還原、三種格式匯出內容正確（含透明/不透明 SVG 差異、PNG 檔頭位元組驗證）、匯入 `.mmd` 正確取代畫面，無 console 錯誤
- **已知小瑕疵，先記錄未修**：匯入新檔案後畫布不會自動置中/縮放到內容範圍（`fitView` 只在初次掛載時觸發），如果新圖表的節點超出目前可視範圍，需要使用者手動縮放或平移才看得到。功能本身正確，只是初次呈現不夠貼心，之後有需要可以在匯入後主動呼叫 React Flow 的 `fitView()`

**下一步**：Phase 6 — 整體打磨與部署（依企劃書排定的最後一個階段）

### 2026-07-30 — 完成 Phase 4：Undo/Redo、刪除/複製、Subgraph 群組
- 引入 `zundo`（zustand 的 undo/redo middleware）包住 `graphStore`，只追蹤 `model` 這個欄位；所有既有與新增的 store actions 不用額外改寫就自動支援復原/重做
- 頁首新增「復原」「重做」按鈕，並綁定 `Ctrl+Z` / `Ctrl+Shift+Z` 全域快捷鍵；快捷鍵有做焦點防呆——若使用者當下正在程式碼編輯器或其他輸入框裡打字，會放行給該元件自己的原生 undo，不會被我們的全域快捷鍵搶走
- 新增「複製」「刪除」節點功能，浮動面板的操作區按鈕會依目前選取狀態（單選/多選/未選）自動啟用或停用
- **新增 Subgraph 群組的畫布互動**：可用 Ctrl/Cmd + 點擊多選節點，按「群組」建立子圖表分組；分組會渲染成一個依成員節點目前位置即時計算邊界的黃色虛線框，雙擊框上的標籤可重新命名；選取群組內節點後可按「取消群組」解散。群組的邊界完全是即時運算（不落地存座標/尺寸到資料模型），節點移動時框會自動跟著重新包覆
- 顏色套用也一併升級為支援多選：選多個節點後點色票，會一次套用給所有選取節點（原本只支援單一節點）
- **修正三個實作過程中發現的 bug**：
  1. 群組框一開始完全不可見（`visibility: hidden`）——原因是只給了 CSS `style.width/height`，React Flow 判斷節點是否「已量測完成」看的是節點物件上層的 `width`/`height` 欄位，改成直接設定這兩個欄位後才正常顯示
  2. 群組標籤怎麼雙擊都無法進入重新命名模式——追查後發現 React Flow 的節點包裹層會攔截原生 `dblclick` 事件、不讓它往上冒泡到 React 的合成事件系統，導致 `onDoubleClick` 永遠不會觸發；改用「兩次 `onClick` 在 400ms 內視為雙擊」的手動判斷後解決
  3. `removeNode` 內部誤把 `dropEmptySubgraphs()` 的回傳值（完整 Model）直接當成要 `set()` 的 store 狀態（少包了一層 `{ model: ... }`），型別檢查前就先手動抓到並修正
- 已用 Playwright 完整驗證：群組建立/渲染/重新命名/取消群組、複製、刪除、Undo/Redo 按鈕與鍵盤快捷鍵、編輯器聚焦時鍵盤快捷鍵防呆，皆正常運作，無 console 錯誤

**下一步**：Phase 5 — 匯出功能（`.mmd` / SVG / PNG，含透明背景）與 localStorage 自動暫存

### 2026-07-30 — 完成 Phase 3：Mermaid 程式碼反向解析與畫布同步
- 程式碼面板改為可編輯（`@uiw/react-codemirror` 的 `editable` 開放），使用者可直接手打 Mermaid 語法
- 新增 `src/parse/mermaidToModel.ts`：逐行比對正規表示式的子集解析器，支援：`flowchart`/`graph` 標頭與方向（LR/TD，TB 視為 TD，RL/BT 標警告）、6 種節點形狀（含裸字與加引號兩種標籤寫法）、subgraph 區塊（不支援巢狀，巢狀內容併入外層並標警告）、三種連線寫法（`-->`、帶文字 `-- 文字 -->`、pipe 語法 `-->|文字|`，虛線同理）、`style id fill:` 顏色語法。無法辨識的行會加入警告清單並跳過，不中斷其餘行的解析
- 解析時會比對「前一個版本的 Model」：已存在的節點沿用原本畫布座標與顏色（避免每次打字節點就跳來跳去），新出現的節點則用簡單的卡片式排列給預設座標；連線也盡量沿用原本的 id 以維持穩定
- 程式碼面板改用「本地文字狀態 + 反饋迴圈防呆」：只有當 Model 的變動不是來自程式碼面板自己（例如畫布拖曳、選色）時，才會用重新序列化的結果覆蓋掉使用者正在打的文字，避免使用者打字打到一半畫面被格式化跳掉
- 輸入變更會 debounce 500ms 才觸發解析，避免打每個字都重新解析、閃爍畫布
- 無法解析的行會在程式碼面板下方顯示可展開的警告清單（第幾行、原始內容），其餘可解析的行仍正常同步到畫布與預覽，符合先前談好的「不中斷整體編輯」原則
- 已用 Playwright 驗證完整雙向流程：在程式碼裡改既有節點文字、新增全新節點與連線（含 pipe 語法）、刻意打一行錯誤語法，確認畫布/預覽正確同步、警告正確顯示且不影響其餘內容，無 console 錯誤

**下一步**：Phase 4 — Undo/Redo、刪除/複製節點、Subgraph 群組的畫布互動（目前 subgraph 只能透過程式碼編輯設定）

### 2026-07-30 — 版面再改版：三欄＋浮動形狀顏色面板、節點顏色功能
- 版面從「形狀｜畫布｜程式碼｜預覽」四欄，改為「程式碼｜畫布｜預覽」三欄可拖拉調整寬度面板，預設比例 25% / 25% / 50%
- 形狀選單移出獨立欄位，改成浮動疊層：預設收合成一個小標籤，重疊在畫布面板左上角，點擊展開浮動卡片；不再佔用固定欄寬
- 新增「重設版面」按鈕（頁首右上角），點擊會用 `react-resizable-panels` 的 `Group` 命令式 API 把三欄寬度復原成預設比例，並清掉 localStorage 裡記住的版面
- 「體育場形」更名為「膠囊形（開始／結束）」，避免跟既有的「圓角矩形」形狀撞名
- **新增節點顏色功能**（原企劃書列為 MVP 不做的項目，這次正式納入）：選取畫布上的節點後，可在浮動面板的顏色區點選 8 色色票（含一個清除顏色選項）套用到該節點。顏色會存進 Graph Model，序列化時輸出對應的 Mermaid `style 節點ID fill:#顏色` 語法，畫布與預覽面板同步顯示套色結果
- 過程中抓到並修正一個因這次改動才浮現的既有 bug：畫布節點資料每次因 Model 變更（例如套用顏色）重新從 store 生成時，會整批覆寫本地 React Flow 節點狀態，導致節點的「已選取」狀態被重置——這讓「選節點→連續點好幾個顏色試效果」的操作在點第一個顏色後就失效。修正為重新生成節點時保留原本被選取的節點 id
- 過程中也發現一個 UI 死角並修正：展開浮動面板時，若畫布上剛好有節點被面板疊住，會擋住點擊；測試時改為「先選節點、再展開面板」的操作順序驗證，此為浮動疊層設計的預期取捨（已跟需求方向一致，非 bug）
- 已用 Playwright 完整驗證：面板改名、色票停用/啟用狀態、套色與清除顏色、程式碼與預覽同步、重設版面按鈕，皆正常運作，無 console 錯誤

**下一步**：Phase 3 — 實作 Mermaid 程式碼反向解析器（子集語法），讓使用者直接編輯程式碼時能同步更新畫布

### 2026-07-30 — 版面改版：可拖拉調整／可收合面板
- 起因：原本的三欄 CSS Grid 版面把「程式碼」與「預覽」硬性平分同一欄，導致最重要的預覽區塊被擠得很小，整體也偏「試算表」的呆板感
- 引入 `react-resizable-panels`，改為 形狀｜畫布｜程式碼｜預覽 四塊可拖拉調整寬度的面板；預設比例為預覽最大（50%）、畫布次之（32%）、程式碼預設收合成一條可點擊展開的直式標籤（4%）、形狀面板固定窄欄（14%）
- 面板寬度與收合狀態透過 `useDefaultLayout` 自動存入 `localStorage`，下次開啟自動還原
- 除錯過程中發現一個容易誤踩的陷阱：`Panel` 的 `defaultSize`/`resize()` 若傳數字會被解讀成「像素」而非「百分比」，導致收合／展開幾乎沒有視覺差異；改用百分比字串（如 `"24%"`）後才正確運作
- 順手做了一輪整體視覺打磨：面板改成白底、圓角、淺陰影的卡片樣式，分隔線改為可拖拉的小握把、頁首加上陰影分層、形狀面板卡片加上 hover 回饋
- 已用 Playwright 驗證：程式碼面板預設收合／點擊展開／拖曳分隔線調整寬度／重新整理後版面還原，皆正常運作，無 console 錯誤

**下一步**：Phase 3 — 實作 Mermaid 程式碼反向解析器（子集語法），讓使用者直接編輯程式碼時能同步更新畫布

### 2026-07-30 — 完成 Phase 2：React Flow 畫布拖拉編輯
- 整合 `@xyflow/react`，畫布改由 Graph Model 驅動：本地 React Flow 節點/連線狀態與 Zustand store 雙向同步（拖曳過程只更新畫布本地狀態以維持流暢度，放開滑鼠才寫回 store，避免每個像素都觸發 Mermaid 重新渲染）
- 實作自訂節點元件 `ShapeNode`，依 6 種形狀（矩形／圓角矩形／圓形／菱形／體育場形／資料庫圓柱）套用對應 CSS 外觀；每個節點四個方向都有來源/目標控點供拉線連接
- 左側形狀面板改為可拖拉來源，放到畫布上會依放置座標新增節點（`addNode`）
- 雙擊節點文字即可就地編輯，Enter 確認、Esc 取消，變更會寫回 store 並即時反映在程式碼與預覽面板
- 節點/連線的刪除（React Flow 內建的 Delete 鍵行為）也已同步寫回 store，避免畫布與資料模型不一致
- **修正一個顯示問題**：菱形節點原本用 CSS `clip-path` 裁切矩形邊框做出菱形外觀，但在寬扁比例下邊框幾乎完全不可見（clip-path 與淺色細邊框的已知相容性問題）。改用內嵌 SVG 圖形當背景圖（`preserveAspectRatio="none"` 讓多邊形依節點寬高拉伸），邊框穩定可見
- 已用 Playwright 模擬完整互動流程實測：拖拉新增節點、雙擊改文字、用控點拉線連接兩個節點，並確認程式碼面板與預覽面板都正確反映每一步操作，無 console 錯誤

**下一步**：Phase 3 — 實作 Mermaid 程式碼反向解析器（支援子集語法），讓使用者直接編輯程式碼時能同步更新畫布，無法解析的部分以警告標示但不中斷整體編輯

### 2026-07-30 — 完成 Phase 1：Graph Model 與 Mermaid 序列化、預覽渲染
- 定義 `GraphModel` 資料型別（`src/types/graph.ts`）：節點（含 6 種形狀）、連線（實線/虛線＋文字標籤）、Subgraph、排版方向，作為畫布與程式碼雙向同步的單一事實來源
- 實作 `graphModelToMermaid()` 序列化器（`src/serialize/modelToMermaid.ts`），將資料模型轉為 Mermaid flowchart 語法，正確處理形狀符號、標籤跳脫（雙引號轉 `#quot;`）、Subgraph 區塊縮排
- 串接官方 `mermaid.js` 渲染引擎（`src/mermaid/render.ts`），預覽面板改為即時渲染真實 Mermaid 輸出，渲染失敗時顯示錯誤訊息而非白畫面
- 用 `zustand` 建立全域 `graphStore`，並放入一組範例資料（開始→輸入→判斷→子流程→結束）作為目前的展示內容，之後 Phase 2 會改由畫布拖拉操作驅動
- 移除未使用的 `@codemirror/lang-markdown` 依賴
- 已用 Playwright 實測：程式碼面板正確顯示產生的 Mermaid 語法、預覽面板正確渲染出含 Subgraph／判斷菱形／資料庫圓柱／虛線標籤連線的圖形，無 console 錯誤

**下一步**：Phase 2 — 整合 React Flow，實作形狀面板拖出節點、雙擊編輯文字、畫布連線繪製，讓畫布操作能實際寫回 Graph Model

### 2026-07-30 — 完成 Phase 0：專案骨架與三欄基礎版面
- 用 Vite 建立 React + TypeScript 專案骨架，命名為 `mermaidgenerator`
- 安裝核心依賴：`@xyflow/react`（畫布）、`mermaid`（渲染）、`zustand`（狀態管理）、`@uiw/react-codemirror`（程式碼編輯器）、`uuid`
- 建立三欄式版面骨架：左側形狀面板（矩形／圓角矩形／圓形／菱形／體育場形／資料庫圓柱佔位項目）、中間畫布區、右側程式碼面板 + Mermaid 預覽面板堆疊
- 移除 Vite 預設的展示頁內容與未使用素材，介面文字改為繁體中文
- 已用 Playwright 啟動 headless Chromium 實測畫面，確認渲染正常、無 console 錯誤

**下一步**：Phase 1 — 定義 Graph Model 資料結構、實作 Model → Mermaid 序列化器，並串接 mermaid.js 讓預覽面板顯示真實渲染結果
