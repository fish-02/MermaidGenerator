# MermaidGenerator 介面重構與功能升級規格

## 1. 文件用途

本文件是 `fish-02/MermaidGenerator` 的設計與實作交接規格。執行者應先完整閱讀本文件及現有程式碼，再開始修改。

本次目標不是單純更換色彩，而是重構資訊架構、工作流程、視覺系統、互動狀態與響應式體驗，同時保留既有 Mermaid 編輯、畫布操作、預覽和匯出能力。

實作前應先盤點現有技術架構、資料模型、Mermaid 與 React Flow 的同步方式、支援的 Mermaid 類型，以及現有測試與部署流程。若規格與技術能力衝突，應先提出具體影響與最小替代方案，不可靜默刪減功能。

## 2. 產品定位

### 2.1 主要使用者

主要面向熟悉 Mermaid、希望快速進行視覺化編輯的使用者。介面可以保持專業與高效率，但仍須讓工作模式、錯誤狀態及畫布支援範圍容易理解。

### 2.2 體驗原則

- 現代、極簡、乾淨、整潔、優雅。
- 畫布是預設核心工作區。
- 程式碼與最終 Mermaid 預覽是可依情境切換的輔助工作區。
- 常用操作直接顯示；情境操作依選取狀態出現；低頻操作收進更多選單或指令面板。
- 不以增加畫面元素的方式展現功能豐富度。
- 介面主題不得影響匯出的 Mermaid 圖表主題。
- 桌面、平板與手機使用適合各自空間的結構，不可將桌面三欄直接壓縮到小螢幕。

## 3. 現況問題

目前桌面版將程式碼、互動畫布、最終預覽同時並列。功能雖完整，但存在以下問題：

- 三個工作區權重相近，主要工作焦點不明。
- 「畫布」與「Mermaid 預覽」的角色差異不夠清楚。
- 形狀、顏色與節點操作集中在畫布浮動面板，資訊密度與狀態層級不足。
- 匯入與匯出入口分散。
- 頁首占用空間，但關鍵操作缺乏清楚層級。
- 缺少完整的錯誤、儲存、載入、空白、成功與不支援狀態。
- 色彩操作過度依賴色塊與滑鼠提示。
- 畫布控制器語言及 React Flow 品牌標記與整體產品感不一致。
- 768px 寬度仍維持三欄，各欄過窄。
- 手機寬度仍維持三欄並產生水平溢出，實際無法操作。

## 4. 資訊架構與工作模式

提供三種桌面工作模式，模式切換置於頂部工具列中央。

### 4.1 畫布模式（預設）

- 大面積互動畫布。
- 左側顯示可開合的建立／屬性欄。
- 最終預覽可由右側面板展開。
- 適合拖曳建立、編輯及排列節點。

### 4.2 程式碼模式

- 放大 Mermaid 程式碼編輯器。
- 旁邊保留縮小的最終 Mermaid 預覽。
- 預設隱藏建立／屬性側欄。
- 不需同時顯示互動畫布。

### 4.3 對照模式

- 程式碼、互動畫布、最終預覽三欄並列。
- 各欄寬度可拖曳調整。
- 預設顯示窄版建立／屬性側欄，可自行展開。
- 空間不足時不可把三欄壓縮成不可用尺寸；應提示切換為頁籤式布局。

### 4.4 工作模式狀態

- 記住使用者最後使用的模式。
- 模式切換需保持內容、選取物件、畫布視角及未完成編輯狀態。
- 動畫採短暫淡入或小幅滑動，不讓主要內容跳動。

## 5. 頂部工具列

將現有大型頁首改為精簡的單列工具列。

### 5.1 左側

- 抽象「節點＋連線」品牌圖示。
- 可直接編輯的檔案名稱，預設為「未命名流程圖」。
- 儲存狀態：`儲存中…`、`已儲存`、`儲存失敗`。

### 5.2 中央

- 畫布模式。
- 程式碼模式。
- 對照模式。

### 5.3 右側

- 復原、重做：純圖示，含名稱與快捷鍵提示。
- 匯入。
- 匯出：保留文字標籤，作為主要操作。
- 更多選單。

「重設版面」移入更多選單。低頻設定與說明亦放入更多選單或指令面板。

## 6. 畫布與左側建立／屬性欄

### 6.1 整體結構

左側欄採上下布局：

- 上方：建立工具。
- 下方：屬性設定。
- 左欄整體可拖曳調整寬度。
- 上下區域之間可拖曳調整高度比例。
- 左欄可完全收合，只留下明確的展開控制。
- 系統記住寬度、高度比例及收合狀態。
- 設定合理最小／最大尺寸，避免任何區域縮成不可操作。
- 上方與下方應可個別收合，以因應視窗高度不足。

### 6.2 建立工具

- 支援精簡圖示網格與「圖示＋名稱」清單切換。
- 預設為精簡圖示網格。
- 滑鼠停留或鍵盤聚焦時，顯示名稱及對應 Mermaid 語法。
- 點擊形狀：在目前畫布視野中央建立節點。
- 拖曳形狀：在放開位置建立節點。
- 提供搜尋。
- 最近使用的形狀優先顯示。
- 形狀圖示需直接反映實際幾何形狀。

### 6.3 屬性設定

依目前選取狀態動態顯示：

- 節點：文字、形狀、填色、邊框、文字色、大小。
- 連線：線型、粗細、顏色、箭頭、標籤。
- 群組：名稱、背景、邊框、內距。
- 未選取物件：圖表方向、輸出主題、背景、間距、全域字體。

Mermaid ID 可直接修改，但放在「進階」折疊區。修改 ID 前需驗證唯一性；可能影響連線時要清楚提示，不可造成靜默資料損壞。

### 6.4 新增節點後的行為

1. 新節點立即被選取。
2. 節點文字立即進入編輯狀態。
3. 屬性區同步顯示該節點設定。
4. `Enter` 完成文字編輯。
5. `Esc` 取消文字編輯。
6. 點擊新增時置於目前畫布視野中央。
7. 拖曳新增時置於放開位置。

## 7. 響應式布局

斷點以可用空間為主：

- `≥1200px`：完整桌面布局，可使用三欄對照模式。
- `768–1199px`：頁籤切換布局；建立／屬性使用左側抽屜。
- `<768px`：手機頁籤布局；建立／屬性使用底部面板。
- `<480px`：進一步精簡頁首；檔名截斷，次要操作收進更多選單。

其他要求：

- 平板與手機主要頁籤為「畫布／程式碼／預覽」。
- 不因裝置名稱或方向硬性固定布局；橫向手機若空間足夠可採平板布局。
- 手機底部面板可拖曳調整高度，亦須提供明確展開／關閉按鈕。
- 手機定位為簡化編輯：可查看、匯出，並修改文字、形狀、顏色與位置。
- 群組、多選及複雜連線等進階操作可限制在較大螢幕，但須清楚說明，不可顯示無反應控制。
- 不得產生非必要水平頁面捲動。
- 主工具、頁籤、匯出及錯誤訊息在所有支援尺寸均須可操作。

## 8. 視覺系統

### 8.1 風格

以冷靜專業為主，融入柔和精緻的溫度：

- 中性灰白或暖白背景。
- 面板可使用極淡米灰。
- 正文使用接近墨黑的深色，不使用純黑。
- 不使用低對比淺灰作為主要正文。
- 避免大量玻璃模糊、漸層、厚重陰影與過多懸浮卡片。

### 8.2 品牌與狀態色

- 主要互動色：低飽和霧藍，用於主要按鈕、選取框、連線控制點與焦點框。
- 輔助狀態色：鼠尾草綠，用於已儲存、有效及完成。
- 危險操作：低飽和磚紅。
- 警告：柔和赭黃。
- 顏色不得是傳達狀態的唯一方式。

### 8.3 主題

- 明亮模式。
- 深色模式。
- 跟隨系統，作為預設值。
- 記住使用者選擇。
- 介面主題與 Mermaid 輸出主題完全獨立。

### 8.4 密度

- 舒適與緊湊兩種密度。
- 桌面預設舒適。
- 對照模式可採略緊湊的布局規格，但不可偷偷改寫使用者的全域偏好。

### 8.5 元件造型

- 工具列及面板主要使用細分隔線，不將每個區域做成卡片。
- 按鈕及輸入框約 8px 圓角。
- 選單、浮層及底部面板約 12px 圓角與柔和陰影。
- 主畫布保持大面積平面，不使用厚重外框。
- 分隔線平時低調，滑入、鍵盤聚焦或拖曳時以主要互動色強調。
- 使用線寬一致的 SVG 圖示，不使用 Emoji。

### 8.6 動效

- 整體採克制、細緻的微動效。
- 側欄展開／收合約 180–220ms。
- 模式及頁籤切換採淡入或短距離滑動。
- 選取節點時只改變外框、控制點與屬性內容，不讓節點跳動。
- 儲存成功使用低干擾文字狀態，不重複顯示大型通知。
- 尊重 `prefers-reduced-motion`。

## 9. 單文件與本機儲存

### 9.1 儲存模型

- 不需要登入或後端。
- 僅維護一份目前文件，不提供多份本機文件管理。
- 自動保存 Mermaid 內容、節點位置、目前工作模式、側欄尺寸及收合狀態。
- 重新開啟網站時恢復目前文件。
- `Ctrl/Cmd + S` 不下載檔案；只顯示「已自動儲存」。
- `.mmd` 下載是主要可攜式備份方式。

### 9.2 資料風險說明

介面應明確說明：

- 清除網站資料會刪除目前文件。
- 無痕模式可能無法永久保留。
- 更換瀏覽器或裝置不會自動同步。
- 分享連結或 `.mmd` 檔案可用於跨裝置傳遞。

自動儲存不可完全關閉。若提供設定，僅允許調整合理的保存頻率。

### 9.3 取代目前文件的保護

空白流程圖、範例或匯入皆會取代目前文件，因此必須：

- 先顯示確認視窗。
- 明確告知目前內容將被取代。
- 提供「先下載目前 `.mmd`」快捷操作。
- 確認後才取代。
- 不提供「不要再詢問」。
- 新文件建立後提供一次性的「復原上一份文件」。
- 復原能力保留到新文件再次被編輯或頁面關閉為止。

## 10. 新文件、空白狀態與範例

### 10.1 首次使用

首次進入顯示精簡空白引導，不直接載入大型範例：

- 標題：「建立第一張流程圖」。
- 主要按鈕：「空白流程圖」。
- 次要按鈕：「查看範例」。
- 入口：「貼上 Mermaid 程式碼」。
- 提示可拖入 `.mmd`。
- 可選擇不再顯示簡短新手提示。

### 10.2 建立入口

提供「空白流程圖及範例」按鈕；使用者需要時才展開建立介面。建立方式包括：

- 空白流程圖。
- 從範例建立。
- 匯入 `.mmd` 或文字檔。
- 貼上 Mermaid 程式碼。

空白文件預設為 `flowchart TD`；方向稍後可在圖表屬性中修改。

### 10.3 第一版範例

準備約 8–12 個高品質範例，可包含：

- 基本流程圖。
- 判斷流程。
- 泳道概念流程。
- 系統架構。
- 使用者旅程。
- 狀態轉換。
- 序列圖。
- 類別圖。
- ER 圖。
- Git 分支圖。

能力分級：

- 流程圖類型提供完整畫布編輯。
- 其他 Mermaid 類型第一版可由範例開啟並在程式碼模式編輯。
- 每個範例清楚標示「支援畫布編輯」或「程式碼編輯」。
- 不可讓使用者誤以為所有 Mermaid 類型都支援視覺化拖拉。

## 11. 匯入

支援：

- 選擇 `.mmd` 或文字檔。
- 將檔案直接拖入網站。
- 貼上 Mermaid 程式碼。
- 匯入前先檢查語法。

匯入永遠取代目前文件，不建立第二份本機文件。必須套用第 9.3 節的覆寫保護。

若內容是有效 Mermaid、但只有部分支援畫布編輯：

- 完整保留原始程式碼。
- 顯示警告而非錯誤。
- 自動切換到程式碼模式。

## 12. 匯出、複製與分享

### 12.1 匯出選單

右上角「匯出」為主要按鈕，選單包含：

- 複製 Mermaid 程式碼。
- 複製 SVG。
- 複製 PNG。
- 下載 `.mmd`。
- 下載 SVG。
- 下載 PNG。
- 匯出設定。

### 12.2 匯出設定

- 透明或實色背景。
- Mermaid 輸出主題。
- PNG 1×、2×、3×。
- 圖像邊距。
- 自訂背景色。
- 第一版不需要任意像素尺寸。

複製成功時，在觸發按鈕附近顯示短暫回饋，例如「已複製 SVG」，不要使用大型通知。

### 12.3 網址內嵌分享

第一版加入不需後端的網址內嵌分享：

- 建立連結前提示：「內容會包含在連結中，請勿分享敏感資料。」
- 若網址超過合理長度，停止或避免產生不可用連結，改建議下載 `.mmd`。
- 解碼失敗或內容損毀時提供可理解的錯誤與返回方式。

## 13. Mermaid 語法錯誤與預覽狀態

錯誤採三層呈現：

1. 編輯器內：錯誤行底線及行號標記。
2. 編輯器下方：簡短錯誤訊息及跳至錯誤位置。
3. 畫布／預覽：保留最後一次有效結果，顯示「預覽尚未更新」。

其他要求：

- 點擊錯誤訊息可定位游標。
- 指令面板提供「前往下一個錯誤」。
- Mermaid 本身有效但無法視覺化編輯時，使用警告而非錯誤。
- 使用者輸入暫時不完整時，不清空整個畫布或預覽。
- 警告與錯誤須可被輔助技術讀取。

## 14. 指令面板與快捷鍵

### 14.1 指令面板

由 `Ctrl/Cmd + K` 開啟，支援模糊搜尋、最近使用及鍵盤上下選擇。第一版至少包含：

- 切換工作模式。
- 新增節點或群組。
- 開啟範例。
- 匯入、匯出及複製。
- 自動排列（若通過技術驗證）。
- 適應畫布。
- 開啟設定。
- 切換明暗模式。
- 查看所有快捷鍵。
- 前往下一個錯誤。

手機不顯示鍵盤快捷鍵字樣，但可由更多選單開啟指令搜尋。

### 14.2 快捷鍵

- `Ctrl/Cmd + Z`：復原。
- `Ctrl/Cmd + Shift + Z`：重做。
- `Ctrl/Cmd + K`：指令面板。
- `Ctrl/Cmd + S`：顯示「已自動儲存」。
- `Ctrl/Cmd + C/V`：複製／貼上所選節點。
- `Delete/Backspace`：刪除選取內容。
- `Enter`：編輯節點文字或確認目前編輯。
- `Esc`：取消、關閉浮層或退出目前操作。
- `F`：適應畫布。
- `+/-`：縮放。
- `?`：快捷鍵說明。

快捷鍵必須依焦點情境執行。程式碼編輯器或文字欄位取得焦點時，不得攔截正常文字編輯操作。

## 15. 設定

設定視窗保留需持久化的選項：

- 介面主題：跟隨系統／明亮／深色。
- 介面密度：舒適／緊湊。
- 編輯器字級。
- 編輯器自動換行。
- 顯示小地圖。
- 顯示畫布格線。
- 自動儲存頻率；不可完全關閉。
- 動效：完整／減少。
- 恢復預設布局。
- 清除本機資料。

輸出主題、輸出背景及圖像設定不放在全域介面設定，應位於圖表屬性或匯出設定。

「清除本機資料」是破壞性操作，必須清楚列出影響、二次確認，並提供先下載 `.mmd` 的入口。

## 16. 無障礙與觸控驗收

- 正文與控制項符合 WCAG AA 對比。
- 所有核心操作均可使用鍵盤完成。
- 焦點框清楚可見。
- 不以顏色作為唯一訊息。
- 純圖示按鈕提供可存取名稱、提示及快捷鍵資訊。
- 桌面點擊目標至少約 36px。
- 觸控點擊目標至少 44px。
- 色彩選擇顯示色名，不只顯示色塊。
- 拖曳操作皆有非拖曳替代方式。
- 錯誤、儲存、複製及匯出結果可被輔助技術讀取。
- 模態視窗與抽屜需正確管理焦點、支援 `Esc` 關閉並在關閉後還原焦點。
- 支援系統減少動態效果設定。
- 不可移除瀏覽器縮放能力。

## 17. 瀏覽器支援

第一版支援目前版本及前一個主要版本的：

- Chrome。
- Edge。
- Firefox。
- Safari。

若某項剪貼簿或圖像功能受瀏覽器限制，須提供下載等替代路徑，並顯示具體說明。

## 18. 功能分期

### 18.1 第一版必做

- 三種工作模式。
- 完整響應式重構。
- 左側上下建立／屬性區。
- 單文件自動儲存及覆寫保護。
- 程式碼錯誤定位及最後有效預覽。
- 複製及下載 Mermaid、SVG、PNG。
- 範例入口及 Mermaid 類型支援標示。
- 指令面板及核心快捷鍵。
- 明亮／深色／跟隨系統介面主題。
- 舒適／緊湊介面密度。
- 輸出主題及 PNG 倍率。
- 網址內嵌分享。
- 無障礙及觸控基本要求。

### 18.2 技術驗證後決定是否納入第一版

- 自動排列。
- 複製 PNG 到系統剪貼簿。
- 非流程圖類型的完整畫布編輯。

若驗證後不納入，須保留清楚的替代流程，並記錄限制與後續建議。

### 18.3 第二版候選

- 雲端分享。
- 多文件管理。
- 跨裝置同步。
- 帳號系統。
- 即時協作。

不可在第一版中為第二版功能預留造成混淆或無法使用的入口。

## 19. 實作流程要求

1. 讀取現有專案說明、`AGENTS.md`、套件設定與相關原始碼。
2. 確認目前 Git 狀態，保留使用者既有修改。
3. 盤點 Mermaid 解析、React Flow 資料同步、匯入、匯出及持久化實作。
4. 先列出實作計畫與技術風險，再開始修改。
5. 優先建立穩定的布局、狀態模型及設計 token，避免大量零散 CSS 修補。
6. 保留既有可用功能，除非本規格明確改變其流程。
7. 對高風險能力先做小型技術驗證。
8. 實作後執行格式、型別、單元、整合及建置檢查；依專案現有工具為準。
9. 以實際瀏覽器驗證桌面、平板及手機版，不只依靠靜態程式碼判斷。
10. 部署不包含在預設授權內；完成本機實作與驗收後，另行取得部署授權。

## 20. 最低測試矩陣

### 20.1 視窗尺寸

至少驗證：

- 1440×900。
- 1280×720。
- 1024×768。
- 768×1024。
- 390×844。
- 360×800。
- 橫向手機。

### 20.2 核心流程

- 首次空白引導。
- 建立空白流程圖。
- 從範例取代目前文件。
- 匯入 `.mmd`、拖入檔案及貼上程式碼。
- 覆寫確認、先下載及一次性復原。
- 畫布新增、拖曳、選取、編輯、複製及刪除節點。
- 左欄調整寬度、高度、收合及狀態恢復。
- 三種工作模式切換及狀態保持。
- 有效 Mermaid、暫時語法錯誤、無法視覺化但有效的 Mermaid。
- 自動儲存、重新載入恢復及儲存失敗。
- 複製／下載 Mermaid、SVG、PNG。
- PNG 1×、2×、3×及背景設定。
- 網址分享、過長內容及損毀連結。
- 明亮、深色、跟隨系統與減少動效。
- 指令面板及所有核心快捷鍵。
- 鍵盤完整操作及焦點管理。

### 20.3 回歸條件

- Mermaid 程式碼、畫布與最終預覽保持一致。
- 語法錯誤不清空最後有效預覽。
- 模式或斷點切換不遺失內容。
- 沒有非必要水平頁面捲動。
- 面板不可縮到無法操作。
- 匯入、新建或清除資料不可未經確認覆寫內容。
- 自動儲存不造成明顯輸入延遲。

## 21. 完成定義

僅在以下條件均滿足時，才能視為第一版完成：

- 第一版必做功能已實作或有經確認的例外。
- 技術驗證項目已有結果、證據及取捨說明。
- 支援尺寸下的核心流程可完成。
- 無已知阻斷性操作問題、內容遺失問題或嚴重無障礙問題。
- 測試與正式建置通過。
- 已提供變更摘要、測試結果、已知限制及部署前注意事項。
- 未在未授權情況下部署或變更遠端環境。

## 22. 給 VS Code Codex 的建議起始指令

將本文件放入專案根目錄後，可使用以下指令開始：

> 完整閱讀 `MermaidGenerator_REDESIGN_SPEC.md`、專案內的 `AGENTS.md` 與現有原始碼。先盤點目前架構、Git 狀態、現有功能、測試方式，以及 Mermaid 和 React Flow 的同步限制。請先提出分階段實作計畫與技術風險；確認規格可行後，依第一版必做範圍完成介面重構、功能實作與響應式／無障礙測試。保留所有不相關的既有修改，不要部署，也不要擅自刪減規格；遇到實質衝突時先說明影響與最小替代方案。

