# MermaidGenerator — 專案企劃書

## 1. 專案定位

一個「免打程式碼」的線上 Mermaid 流程圖編輯工具。使用者可以透過拖拉、點擊的方式在畫布上建立流程圖，工具會即時產生對應的 Mermaid 程式碼；反過來，使用者也可以直接編輯程式碼，畫布會即時同步更新。最終可匯出 Mermaid 原始碼、SVG、PNG。

- **用途**：課程作業 / 展示 / Portfolio 專案
- **部署型態**：純前端靜態網站，無登入、無後端、無資料庫
- **介面語言**：繁體中文，單一主題（不做深色模式）
- **時程**：無截止日期，採階段式持續開發

## 2. MVP 功能範圍

### 2.1 圖表類型
- ✅ 流程圖（Flowchart / Graph），支援 `LR`（水平）與 `TD`（垂直）排版方向
- ✅ Subgraph 子圖表群組（用於表達簡易的架構分組，取代正式的 `architecture-beta` 語法）
- ❌ 循序圖（Sequence Diagram）— 不在 MVP 範圍，列為未來擴充項目
- 📝 Mermaid 官方 `architecture-beta` 語法（service / group / junction / icon）— 不在 MVP 範圍，但需求細節記錄於本文件附錄，供日後評估是否加入

### 2.2 節點形狀
- 矩形 `[ ]`
- 圓角矩形 `( )`
- 圓形 `(( ))`
- 菱形（判斷）`{ }`
- 體育場形／圓角膠囊 `([ ])`
- 資料庫圓柱 `[( )]`

MVP 統一使用預設樣式（固定配色與邊框），**不提供**自訂填色 / 邊框顏色 — 列為未來擴充項目。

### 2.3 連線
- 實線箭頭 `-->`
- 虛線箭頭 `-.->`
- 帶文字標籤（`-- 文字 -->` / `-. 文字 .->`）

### 2.4 編輯操作
- 拖拉節點到畫布、雙擊編輯文字
- 節點 / 連線刪除、複製
- Undo / Redo（復原 / 重做）
- 雙向同步：畫布操作即時反映到程式碼；程式碼編輯即時反映到畫布
  - 若手動輸入的程式碼包含畫布無法解析的語法（例如尚未支援的節點形狀、特殊語法），該部分在畫布上以警告標示，其餘可解析部分維持正常顯示，不中斷整體編輯

### 2.5 匯出 / 儲存
- 匯出：Mermaid 原始碼（`.mmd`）、SVG（含透明背景選項）、PNG（含透明背景選項）
- 儲存：瀏覽器 `localStorage` 自動暫存目前作品；支援手動匯出 / 匯入 `.mmd` 或 `.json`（內部圖形資料模型）檔案

## 3. 介面配置

三欄式版面（可收合切換，以因應小螢幕）：

```
┌───────────────┬───────────────────┬───────────────────┐
│  左側：形狀面板 │   中間：拖拉畫布     │  右側：程式碼 + 預覽 │
│  （可拖出節點） │  （React Flow）    │  （上下堆疊或分頁）  │
└───────────────┴───────────────────┴───────────────────┘
```

- **畫布**：使用 React Flow 呈現，供拖拉、連線、編輯，視覺風格為編輯器用途，非最終成品外觀
- **程式碼面板**：純文字編輯器（如 CodeMirror），顯示/編輯即時產生的 Mermaid 原始碼
- **預覽面板**：使用官方 `mermaid.js` 實際渲染程式碼，呈現「最終真實輸出」的樣子 — **匯出圖片以此渲染結果為準**，非畫布本身的視覺樣式

## 4. 技術架構

| 項目 | 選擇 | 理由 |
|---|---|---|
| 前端框架 | React + TypeScript | 生態成熟、型別安全，利於維護雙向同步邏輯 |
| 建置工具 | Vite | 純前端靜態網站首選，開發體驗快 |
| 拖拉畫布引擎 | React Flow (`@xyflow/react`) | 現成節點拖拉、連線、縮放平移、Subgraph 群組能力成熟，避免從零手刻互動邏輯 |
| Mermaid 渲染 | `mermaid` 官方套件 | 用官方渲染引擎產生預覽與最終匯出圖像，確保匯出結果與真實 Mermaid 圖表一致 |
| 程式碼編輯器 | CodeMirror 6 | 輕量、可自訂語法高亮與錯誤標示 |
| 全域狀態管理 | Zustand | 輕量、易於管理「單一事實來源」的圖形資料模型並廣播給三個面板 |
| PNG 匯出 | Canvas + `XMLSerializer`（將 mermaid 產出的 SVG 轉繪至 canvas 再輸出 PNG） | 不依賴額外的螢幕截圖服務，純前端可完成 |
| 檔案下載 | `file-saver` 或原生 Blob + `<a download>` | 簡單可靠 |
| 部署 | GitHub Pages 或 Vercel（皆可，靜態網站） | 免費、免維護 |

### 4.1 核心資料流設計（雙向同步的關鍵）

採用「**內部圖形資料模型（Graph Model）為單一事實來源**」的架構，避免畫布與程式碼互相依賴造成同步死結：

```
                ┌─────────────────────┐
                │   Graph Model (JSON) │  ← 單一事實來源
                │  nodes / edges /     │
                │  subgraphs / 排版方向 │
                └──────────┬───────────┘
         ┌──────────────────┼──────────────────┐
         ▼                  ▼                  ▼
   畫布渲染（React Flow）   序列化為 Mermaid 碼    （模型變更時）
         │                  │                  │
   使用者拖拉/編輯 ──────────▶ 更新 Model         Mermaid 官方渲染 → 預覽/匯出
         ▲                  │
         └──────────────────┘
   使用者編輯程式碼 ──解析(Parser)──▶ 更新 Model
```

- **Model → Mermaid 程式碼**：序列化，邏輯單純、可完全掌控，不會出錯
- **Mermaid 程式碼 → Model**：解析，僅針對 MVP 支援的語法子集撰寫輕量解析器（非完整 Mermaid 文法），無法辨識的行以警告標示、其餘正常同步
- **Model → 畫布** 與 **畫布操作 → Model**：透過 React Flow 的節點/邊資料結構直接對應

## 5. 分階段開發規劃（無時間表，依邏輯順序）

1. **Phase 0** — 專案骨架：Vite + React + TS 初始化、基礎版面（三欄）
2. **Phase 1** — 資料模型與序列化：定義 Graph Model schema、Model → Mermaid 序列化器、串接 mermaid.js 預覽渲染
3. **Phase 2** — 畫布拖拉編輯：整合 React Flow、實作形狀面板拖出節點、雙擊編輯文字、連線繪製
4. **Phase 3** — 反向同步：Mermaid 程式碼解析器（子集）、程式碼編輯 → 更新畫布、錯誤/警告標示
5. **Phase 4** — 編輯體驗補強：Undo/Redo、刪除/複製、Subgraph 群組拖拉建立
6. **Phase 5** — 匯出功能：`.mmd` 匯出入、SVG 匯出、PNG 匯出（含透明背景）、localStorage 自動暫存
7. **Phase 6** — 打磨與部署：UI 細節、錯誤處理、部署上線

## 6. 明確排除於 MVP 之外（未來可擴充）

- 循序圖等其他 Mermaid 圖表類型
- 官方 `architecture-beta` 語法（service/group/junction/icon）— 語法細節見附錄
- 節點/連線自訂樣式（顏色、線條粗細）
- 帳號系統、雲端儲存、多人協作、分享連結
- 深色模式
- 行動裝置觸控拖拉優化

## 7. 附錄：Mermaid `architecture-beta` 語法備忘（供未來擴充參考）

```
architecture-beta
    group api(cloud)[API]

    service db(database)[Database] in api
    service disk1(disk)[Storage] in api
    service server(server)[Server] in api

    db:L -- R:server
    disk1:T -- B:server
```

- `group`：分組容器，可含 icon
- `service`：服務節點，格式為 `service <id>(<icon>)[<label>]`，可用 `in <group>` 指定所屬分組
- 連線需指定方位（`T`/`B`/`L`/`R`）決定連接邊
- `junction`：無標籤的分岐點，用於多路連接

該語法目前仍屬 Mermaid 官方 beta 階段，語法與圖示庫可能持續變動，故 MVP 先以「流程圖 Subgraph 分組」做為替代方案。

---

## 8. 自我檢視與風險提醒

- **雙向同步是全案技術難點**：完整支援使用者「手打任意 Mermaid 語法」幾乎等同重寫 Mermaid 的完整 parser，成本過高。已與你確認採「支援子集 + 局部警告」的務實做法，這是目前業界類似工具（如 Mermaid Live Editor 的 Chart 模式）也採用的策略。
- **畫布視覺 ≠ 匯出成品視覺**：因為採用 React Flow 自訂畫布 + Mermaid 官方渲染雙軌並存，兩者外觀不會完全一致。已確認以三欄佈局同時呈現，讓使用者清楚知道「所見即所得」以右側預覽為準，避免誤解。
- **目前規劃未涵蓋行動裝置與無障礙（鍵盤操作）支援**，屬於拖拉編輯器的常見取捨，先聚焦桌面版核心體驗，之後可視情況補強。
- **專案名稱**：MermaidGenerator（已定案）。

## 9. 需求確認清單（已全數確認）

| 項目 | 決議 |
|---|---|
| 部署型態 | 純前端靜態網站 |
| 使用情境 | 課程 / 展示 / Portfolio |
| 畫布技術 | React Flow |
| 前端框架 | React + TypeScript |
| 反向同步錯誤處理 | 局部警告，不中斷整體 |
| 連線類型 | 實線 / 虛線 / 文字標籤 |
| 匯出格式 | SVG + PNG，支援透明背景 |
| 儲存方式 | localStorage 自動暫存 + 手動匯出入 |
| MVP 圖表範圍 | 僅流程圖 + Subgraph（不含循序圖、不含 architecture-beta） |
| 節點形狀 | 矩形／圓角矩形／圓形／菱形／體育場形／資料庫圓柱 |
| 樣式自訂 | MVP 不支援，統一預設樣式 |
| 編輯操作 | Undo/Redo + 刪除/複製 |
| UI 語言/主題 | 繁體中文，單一主題 |
| 介面佈局 | 三欄：畫布 + 程式碼 + 預覽 |
| 時程 | 無期限，分階段開發 |
